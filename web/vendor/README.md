# Vendored frontend assets

Runtime frontend dependencies belong here when added.

The deployed game must not depend on a CDN. If React is used, vendor the pinned browser assets in this directory and document their exact version and license.
